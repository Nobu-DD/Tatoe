<div class="px-4 py-10">
  <div class="bg-white shadow-lg rounded-2xl w-full p-8 space-y-6">
    <div class="text-center mb-5">
      <h2 class=" text-2xl font-bold text-gray-800">お題を投稿</h2>
    </div>

    <%= form_with model: @topic do |f| %>
    <!-- ジャンル選択 -->
    <div class=" mb-5">
      <%= fields_for :genre, @topic.genre do |f| %>
      <%= f.label :genre_names, "お題ジャンル", class: "block text-sm font-medium text-gray-700 mb-3" %>
      <%= f.text_field :name, autofocus: true, class: "input input-info w-full rounded-xl border-gray-200", placeholder: "ジャンルはスペースで区切ってください" %>
      <% end %>
    </div>

    <!-- タイトル -->
    <div class="mb-5">
      <%= f.label :title, "お題タイトル", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <%= f.text_area :title, class: "textarea textarea-info w-full rounded-xl border-gray-200", placeholder: "例：競馬の日本ダービーを別のスポーツで例えると？" %>
    </div>

    <!-- お題の説明 -->
    <div class="mb-5">
      <%= f.label :description, "お題の説明 (任意)", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <%= f.text_area :description, class: "textarea textarea-info w-full rounded-xl border-gray-200", placeholder: "例：競馬の日本ダービーを別のスポーツで例えると？" %>
    </div>

    <!-- ヒント -->
    <div class="mb-5">
      <%= fields_for :hint, @topic.hint do |f| %>
      <%= f.label :body, "ヒント (任意・最大3つ)", class: "block text-sm font-medium text-gray-700 mb-2" %>
      <% 3.times do |i| %>
      <%= f.text_field :body, placeholder: "ヒント#{i + 1}", class: "mb-2 w-full rounded-xl border-gray-200" %>
      <% end %>
      <% end %>
    </div>

    <!-- 送信ボタン -->
    <%= f.submit "お題を作成", class: "w-full bg-[#A0D8EF] hover:bg-[#7CC7E8] text-white py-4 text-lg font-semibold rounded-xl shadow-lg" %>
    <% end %>
  </div>

  <!-- コツセクション -->
  <div class="mt-6 bg-gradient-to-r from-[#F0F9FF] to-[#E0F2FE] rounded-xl p-4 shadow-sm">
    <h3 class="font-semibold text-gray-800 mb-2 flex items-center">
      <svg class="w-4 h-4 mr-2 text-[#A0D8EF]" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" d="M17 8h2a2 2 0 012 2v10a2 2 0 01-2 2H5a2 2 0 01-2-2V10a2 2 0 012-2h2m4-4h4m-4 4V4m0 4h4" />
      </svg>
      良いお題を作るコツ
    </h3>
    <ul class="text-sm text-gray-600 space-y-1">
      <li>• 具体的で分かりやすいタイトルにしましょう</li>
      <li>• ヒントは例えやすくなるような情報を入れましょう</li>
      <li>• 複数のジャンルを選ぶと、より多くの人に見てもらえます</li>
      <li>• AIを活用して、アイデアを膨らませてみましょう</li>
    </ul>
  </div>
  </main>
</div>